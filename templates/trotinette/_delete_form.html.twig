<form method="post" action="{{ path('app_trotinette_delete', {'idTrot': trotinette.idTrot}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trotinette.idTrot) }}">
    <button class="btn">Delete</button>
</form>
